// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void StartWasteSchedulesHandler(ReducerEventContext ctx, SpacetimeDB.Identity participant);
        public event StartWasteSchedulesHandler? OnStartWasteSchedules;

        public void StartWasteSchedules(SpacetimeDB.Identity participant)
        {
            conn.InternalCallReducer(new Reducer.StartWasteSchedules(participant));
        }

        public bool InvokeStartWasteSchedules(ReducerEventContext ctx, Reducer.StartWasteSchedules args)
        {
            if (OnStartWasteSchedules == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnStartWasteSchedules(
                ctx,
                args.Participant
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class StartWasteSchedules : Reducer, IReducerArgs
        {
            [DataMember(Name = "participant")]
            public SpacetimeDB.Identity Participant;

            public StartWasteSchedules(SpacetimeDB.Identity Participant)
            {
                this.Participant = Participant;
            }

            public StartWasteSchedules()
            {
            }

            string IReducerArgs.ReducerName => "start_waste_schedules";
        }
    }
}
